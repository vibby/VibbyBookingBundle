<h1>Event list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Date_from</th>
            <th>Date_to</th>
            <th>Title</th>
            <th>Is_validated</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('event_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{% if entity.datefrom %}{{ entity.datefrom|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{% if entity.dateto %}{{ entity.dateto|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ entity.title }}</td>
            <td>{{ entity.isvalidated }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('event_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('event_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('event_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
